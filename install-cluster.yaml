apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: install-cluster
  namespace: flux-system
spec:
  interval: 5m0s
  path: ./cluster
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-demo
  validation: client
